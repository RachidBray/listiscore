<?xml version="1.0"?>
<ruleset name="ListiScore">
  <file>.</file>
  <exclude-pattern>/vendor/*</exclude-pattern>
  <exclude-pattern>/node_modules/*</exclude-pattern>
  <exclude-pattern>/build/*</exclude-pattern>
  <arg name="extensions" value="php"/>
  <arg name="cache" value=".phpcs-cache"/>
  <rule ref="WordPress">
    <exclude name="WordPress.Files.FileName"/>
  </rule>
  <rule ref="WordPress.WP.I18n">
    <properties>
      <property name="text_domain" type="array">
        <element value="listiscore"/>
      </property>
    </properties>
  </rule>
  <rule ref="WordPress.NamingConventions.PrefixAllGlobals">
    <properties>
      <property name="prefixes" type="array">
        <element value="listiscore"/>
        <element value="LISTISCORE"/>
        <element value="ListiScore"/>
      </property>
    </properties>
    <!-- tests/bootstrap.php and tests/wp-stubs.php intentionally redeclare a
         handful of real WP/GeoDirectory function and constant names as test
         doubles (Brain Monkey mocks the rest); that's the point of a stub,
         not a naming violation. -->
    <exclude-pattern>*/tests/*</exclude-pattern>
  </rule>
  <rule ref="PHPCompatibilityWP"/>
  <config name="testVersion" value="7.4-"/>
</ruleset>
